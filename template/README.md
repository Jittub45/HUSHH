Template folder

This folder contains example templates for a small web app stack:

- Frontend (static HTML + Dockerfile)
- Backend (FastAPI + MongoDB + Dockerfile)
- AWS (CloudFormation sample)
- Monitoring (Prometheus scrape config + Grafana dashboard sample)
- GitHub Actions CI workflow (build + push placeholders)
- Kubernetes manifests (deployments, services, MongoDB PVC)
- MongoDB docker-compose sample

Use these as starting points — replace placeholders (images, secrets, ARNs) with your values.
